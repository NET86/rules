# 日常规则示例：加入已有配置，并在 AI 策略组添加、选择可用节点。
[Proxy Group]
AI = select, REJECT

[Rule]
# 日常 AI 核心域名与 OpenAI 语音 IP 共用同一策略。
# 放在宽泛的 Google、Microsoft、GitHub 和最终匹配规则之前。
RULE-SET,https://raw.githubusercontent.com/NET86/rules/stable/rules/surge/ai-daily.list,AI,update-interval=3600

# REJECT 仅为占位。其余规则和 FINAL 放在下方。
